#334 - Backend Implement Service And Repository

- Disable login submission button until email/username and password inputs are valid.
- Fade/disable forgot-password "Send Reset Link" until the email is valid, and remove the temporary "I have the reset link" shortcut.
- Fade/disable reset/change-password submission until new + confirm passwords are valid and match.
- Hide the home sign-up banner after the user completes sign-up/login from the banner and persist the dismissal while logged in.