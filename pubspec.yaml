name: multichoice
description: "The monorepo for the Multichoice project"
publish_to: "none"

version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  melos: ^4.1.0
  very_good_analysis: ^5.1.0
