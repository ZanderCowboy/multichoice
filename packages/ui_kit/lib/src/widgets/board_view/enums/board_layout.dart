/// Layout orientation for [BoardView].
enum BoardLayout {
  /// Collections arranged as columns; items in each collection scroll vertically.
  vertical,

  /// Collections arranged as rows; items in each collection scroll horizontally.
  horizontal,
}
